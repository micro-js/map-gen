{
  "name": "@f/map-gen",
  "description": "Map for generator.",
  "repository": "git://github.com/micro-js/map-gen.git",
  "version": "1.5.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-generator-object": "^1.1.0",
    "@f/slice": "^1.0.0"
  },
  "devDependencies": {
    "@f/identity": "^1.0.0",
    "standard": "^5.4.1",
    "tape": "^4.2.2"
  }
}
